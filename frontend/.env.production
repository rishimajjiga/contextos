# Production: target the deployed Railway backend directly.
# (Backend CORS is open, so the browser can call it cross-origin.)
# Vercel rewrites in vercel.json remain as a fallback if this is ever blank.
VITE_API_URL=https://api.usecontextos.com
