# Web framework
fastapi==0.111.1
uvicorn[standard]==0.30.1

# Database
sqlalchemy==2.0.31
psycopg[binary]>=3.1.0   # async driver for postgresql+psycopg:// URLs (psycopg3)
asyncpg>=0.29.0           # alternative async driver (postgresql+asyncpg://)
psycopg2-binary>=2.9.9   # sync driver used by Alembic migrations
alembic>=1.13.0          # schema migrations — run on deploy (see Dockerfile CMD)
greenlet>=3.0.0

# Auth
PyJWT==2.8.0
cryptography==42.0.8
httpx==0.27.0             # for fetching Clerk JWKS

# Pydantic / validation
pydantic==2.8.2
pydantic-settings==2.3.4
email-validator==2.2.0

# Billing
razorpay==1.4.2
setuptools>=68.0.0,<74    # required by razorpay on Python 3.12+; <74 keeps pkg_resources.require()

# Rate limiting
slowapi==0.1.9

# Config / logging
python-dotenv==1.0.1
structlog==24.2.0

# PDF generation (Download Your Data export)
reportlab==4.2.2

# Push notifications (Firebase Cloud Messaging)
# <7: firebase-admin 7.x requires httpx>=0.28, which conflicts with the
# supabase 2.5.x stack (httpx<0.28). 6.x has no httpx dependency at all.
firebase-admin>=6.5.0,<7

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
aiosqlite>=0.20.0
